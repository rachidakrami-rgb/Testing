"""ملف تشغيل العيادة"""
from app import app

if __name__ == '__main__':
    # host='0.0.0.0' يجعل الخادم يستمع على كل واجهات الشبكة (وليس فقط
    # 127.0.0.1)، لهذا يمكن الوصول إليه من هاتف على نفس شبكة Wi-Fi عبر
    # عنوان IP المحلي للحاسوب.

    app.run(debug=True, port=5000, host='0.0.0.0')
